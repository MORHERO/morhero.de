extends@: page_base

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            header.content.items:
              type: text
              default: '@self:modular'
              label: pagecoll
            header.content.order.by:
              type: text
              label: orderby
            header.content.order.dir:
              type: text
              label: dir